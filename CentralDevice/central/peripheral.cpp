#include "message.h"
#include "peripheral.h"

BLEDevice central;

void setupPeripheral()
{
  /*
  ntral = BLE.central();
  if (!isCentral())
  {
    // Central does not exits, create it
    BLE.begin();
  }
  */
}